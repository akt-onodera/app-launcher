<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="完了"
    Width="640"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">

    <Window.Resources>
        <!--  MD3-ish colors  -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF6750A4" />
        <SolidColorBrush x:Key="OnPrimaryBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="OnSurfaceBrush" Color="#FF1C1B1F" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#FF5A5661" />

        <Style x:Key="MdFilledButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Height" Value="48" />
            <Setter Property="MinWidth" Value="140" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.86" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.50" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!--  surface  -->
    <Border
        Padding="32"
        Background="{StaticResource SurfaceBrush}"
        CornerRadius="28"
        SnapsToDevicePixels="True">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Title  -->
            <TextBlock
                Grid.Row="0"
                FontSize="28"
                FontWeight="SemiBold"
                Foreground="{StaticResource OnSurfaceBrush}"
                Text="起動が完了しました。" />

            <!--  Body  -->
            <TextBlock
                Grid.Row="2"
                FontSize="15"
                Foreground="{StaticResource SubtleTextBrush}"
                Text="OKボタンを押して、ダイアログを閉じます。"
                TextWrapping="Wrap" />

            <!--  Buttons  -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="okButton"
                    Grid.Column="1"
                    Content="OK"
                    Style="{StaticResource MdFilledButton}" />
            </Grid>
        </Grid>
    </Border>
</Window>
